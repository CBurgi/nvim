return {
  -- cmd = { "phpactor", "language-server" },
  -- filetypes = { "php" },
  -- root_dir = function()
  --   -- Ensure this function returns an absolute path
  --   return vim.fn.getcwd()
  -- end,
  -- on_new_config = function(config, root_dir)
  --   config.cmd_cwd = root_dir
  --   config.cmd_env = vim.fn.environ()
  --   config.root_uri = "file://" .. root_dir -- Set the root URI as absolute
  -- end,
}
